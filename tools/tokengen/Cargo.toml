[package]
name = "tokengen"
description = "Generates platform bindings from the design token source of truth."
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
publish = false

[dependencies]
serde_json = "1"

[[bin]]
name = "tokengen"
path = "src/main.rs"
