[package]
name = "prv-telemetry"
description = "Counts and diagnostics — recorded only with agreement, aggregated, never a trace."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
prv-security = { path = "../prv-security" }

[lints]
workspace = true
